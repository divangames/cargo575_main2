////////////////////////////////////////////////////////
//
// Разбор строки статистики для анимации счётчика
//
////////////////////////////////////////////////////////

/** Достаёт число и признак «+» из значения вроде 120+ */
export function parseStatValue(value: string) {
  return {
    count: Number(value.replace(/\D/g, "")),
    plus: value.includes("+"),
  };
}
